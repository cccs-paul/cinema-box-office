services:
  # PostgreSQL Database
  postgres:
    image: postgres:16-alpine
    container_name: myrc-db
    environment:
      POSTGRES_DB: myrc
      POSTGRES_USER: myrc
      POSTGRES_PASSWORD: myrc_password
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U myrc"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    networks:
      - myrc-network
    restart: unless-stopped

  # Backend API Service
  api:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: myrc-api
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/myrc
      SPRING_DATASOURCE_USERNAME: myrc
      SPRING_DATASOURCE_PASSWORD: myrc_password
      SPRING_JPA_HIBERNATE_DDL_AUTO: validate
      SPRING_FLYWAY_ENABLED: "true"
      SPRING_FLYWAY_BASELINE_ON_MIGRATE: "true"
      SPRING_FLYWAY_BASELINE_VERSION: "0"
      SPRING_PROFILES_ACTIVE: prod
      SPRING_SERVLET_MULTIPART_MAX_FILE_SIZE: 50MB
      SPRING_SERVLET_MULTIPART_MAX_REQUEST_SIZE: 50MB
    ports:
      - "8080:8080"
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8080/api/actuator/health/liveness"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - myrc-network
    restart: unless-stopped

  # Frontend Application
  web:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    container_name: myrc-web
    ports:
      - "80:80"
    depends_on:
      - api
    networks:
      - myrc-network
    restart: unless-stopped

  # pgAdmin for Database Management
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: myrc-pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@example.com
      PGADMIN_DEFAULT_PASSWORD: admin_password
      PGADMIN_CONFIG_ENHANCED_COOKIE_PROTECTION: 'False'
      PGADMIN_CONFIG_CONSOLE_LOG_LEVEL: 10
    ports:
      - "5050:80"
    volumes:
      - ./pgadmin-servers.json:/pgadmin4/servers.json:ro
    depends_on:
      - postgres
    networks:
      - myrc-network
    restart: unless-stopped

volumes:
  postgres_data:
    driver: local

networks:
  myrc-network:
    driver: bridge
